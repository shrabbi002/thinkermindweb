<<<<<<< HEAD
# Thinkers Mind - High-Performance Website

A premium, strategy-first platform built for Clarity and Performance.

## 🚀 Tech Stack
- **Frontend**: React (Vite, TypeScript, Framer Motion, Lucide)
- **Backend**: Node.js (Express, TypeScript, Prisma ORM)
- **Design System**: Glassmorphism, Mesh Gradients, Bento Layouts

## 🛠 Development Setup

### 1. Prerequisite
Ensure you have **Node.js (v18+)** and **Git** installed.

### 2. Local Installation
```bash
# Clone the repository (if not already local)
# git clone <repository-url>

# Install frontend dependencies
npm install

# Install backend dependencies
cd server
npm install
```

### 3. Environment Configuration
- Create a `.env` file in the `server` directory.
- Add your `DATABASE_URL` (PostgreSQL).

### 4. Running the App
```bash
# Root directory (Vite dev server)
npm run dev

# Server directory (Express dev server)
cd server
npm run dev
```

## 🔄 Git Workflow (Daily Push)

To maintain a consistent history, use the following workflow:

1. **Check Status**: `git status`
2. **Stage Changes**: `git add .`
3. **Commit**: `git commit -m "feat: [brief description of changes]"`
4. **Push**: `git push origin main`

> [!TIP]
> Always run `npm run build` before pushing to ensure no build-breaking changes are introduced.
=======
# Thinkers-Mind-website-
>>>>>>> 091706dc62b09cef0b096c2972c604353a6a0809
